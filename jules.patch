diff --git a/proofs/Calibrator/Conclusions.lean b/proofs/Calibrator/Conclusions.lean
index e0a4c37..a57d47a 100644
--- a/proofs/Calibrator/Conclusions.lean
+++ b/proofs/Calibrator/Conclusions.lean
@@ -740,7 +740,7 @@ by
           symm
           exact h_left
         _ = H⁻¹ * (- (Real.exp (rho i) • S_basis i) * beta_hat rho) := by
-          simp [H] using h_mul
+          simpa [H] using h_mul
         _ = -H⁻¹ * ((Real.exp (rho i) • S_basis i) * beta_hat rho) := by
           simp [neg_mul]
     calc
